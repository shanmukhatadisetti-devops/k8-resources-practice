apiVersion: v1
kind: Pod
metadata:
  name: aws-cli
  namespace: roboshop
  labels:
    project: roboshop
    component: aws-cli
    type: testing
spec:
  serviceAccountName: secret-reader
  containers:
  - name: aws-cli
    image: amazon/aws-cli
    imagePullPolicy: Always